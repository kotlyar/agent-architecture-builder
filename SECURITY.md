# Security

Do not report a real credential in a public issue. Revoke or rotate any exposed
credential first, then contact the repository owner privately through the
security-reporting method configured on GitHub.

Generated implementation kits must not contain real `.env` files, authentication
state, private keys, session data, logs, or production databases. External
publication, messages, spending, account changes, deletion, and schedule
activation require separate, exact user authorization.
